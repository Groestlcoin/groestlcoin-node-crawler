# Groestlcoin Node Crawler [WIP]

1. Install the dependencies
    ```bash
    poetry install
    ```
1. Spin up the virtual environment
    ```bash
    poetry shell
    ```
1. Run the crawler
    ```bash
    python example.py
    ```
